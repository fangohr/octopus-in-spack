name: spack-develop

on:
  push:
    branches: [ spack-develop ]
  pull_request:
    branches: [ spack-develop ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: make spack-develop
